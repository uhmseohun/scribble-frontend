import styled from 'styled-components';

const Game = () => {
  return (
    <PageContainer>
    </PageContainer>
  );
};

export default Game;

const PageContainer = styled.div`
  display: flex;
  flex-direction: column;
  height: 100%;
  padding: 10px 50px;
`;
